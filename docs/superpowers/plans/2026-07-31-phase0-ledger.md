# SDD ledger — plan: docs/superpowers/plans/2026-07-31-phase0-act-in-place.md
Task 1: complete (commits 91762c9..62ff14e, review clean)
Task 2: review round 1 — Critical: abs() Path::join drops /sys/fs/cgroup prefix on absolute cg paths (cgfs.rs:171). Fix dispatched to impl-task2.
Task 2: minor (deferred): parse_anon lacks anon_thp-collision regression test (cgfs.rs:258)
Task 2: minor (deferred): implementer skipped TDD red step (process note)
Task 2: fix round 1/5 (1 addressed, 0 open — abs() prefix drop fixed + regression test; commits 17003b8..211cb9b)
Task 2: complete (commits 62ff14e..211cb9b, review clean after 1 fix round)
Task 3: implemented 692bd86 (impl-task3 died at rate limit; impl-task3b audited+completed partial work)
Task 3: review round 1 — Critical: torn-write tail swallows later fsynced entry (no WAL tail recovery in open()); Important: no mutation lock + stable temp filename. Fix dispatched to impl-task3b.
Task 3: minor (deferred): no parent-dir fsync on first-ever journal creation (write_header)
Task 3: minor (deferred): temp filename pid-reuse collision (cosmetic)
Task 3: reviewer cannot-verify (track at integration): daemon call pattern single-threaded?; cgfs read_high trim consistency vs our_high string equality in should_restore
Task 3: fix round 1/5 (2 addressed, 0 open — WAL tail recovery + mutation lock/unique temp names; commits 692bd86..9439119)
Task 3: complete (commits e05d7c2..9439119, review clean after 1 fix round)
Task 4: complete (commits 35ad886..8f4788e, review clean first pass; live freeze/thaw roundtrip verified via journalctl)
Task 4: minor (deferred): run_with_timeout maps Disconnected(panic) to "timed out" message (systemd.rs ~1304)
Task 4: minor (deferred): D-Bus errors reuse Error::Cgroup variant; revisit if branches grow
Task 5: complete (commits a97b196..8b59c41, review clean first pass; effector stopgap graded non-destructive+inert)
Task 5: CARRY-FORWARD to Task 6 (Important): dead-prune LiftCap must also thaw (or emit Thaw+LiftCap for Frozen) — a still-frozen cgroup that transiently fails to resolve must never be dropped from tracking while frozen
Task 5: note: plan test unresolvable_process_is_never_selected corrected in substance (Notify fires at High regardless); plan text had a bug
Task 6: implemented aa08663; review round 1 (opus) — Critical: kernel page-truncation defeats should_restore equality (every cap permanent); Critical: lift ordering raw-restore-then-u64::MAX clobbers user MemoryHigh; Important: single-entry replay + remove-all leaks coexisting entries. All empirically verified. Fix dispatched to impl-task6.
Task 6: minor (deferred): inode unwrap_or(0) poison value — Cap in that state never lifted (effector.rs:95,145)
Task 6: minor (deferred): Journal::open failure silently fatal at daemon startup; boot_id "" self-matches (main.rs:47, cgfs.rs:83)
Task 6: spec errata (mine): carry-forward #2 "byte-identical" unachievable vs page-truncation; "additionally set u64::MAX" ambiguous ordering — plan text was the root cause of both Criticals
Task 6: fix round 1/5 (3 addressed, 1 NEW open — reconcile_our_high remove-then-append durability gap; commits 9203c34..4ec8aac; re-reviewer ran full ignored suite live, 4/4)
Task 6: minor (deferred): cap_page_aligns test can go vacuous on slow machine (assert bytes > MIN_CAP_BYTES)
Task 6: minor (deferred): page-align test is belt-and-braces, not pinpoint (reconcile self-corrects)
Task 6: minor (deferred): RestoreStep::ThawAndRestoreHigh{to} payload vestigial — collapse to unit variant
Task 6: minor (deferred): restore_high_if_any judges liveness on newest entry only ([Cap,Freeze] chain edge)
Task 6: minor (deferred): page_align_down returns 0 for bytes<page — add .max(page) insurance (memory.high=0 would be catastrophic)
Task 6: fix round 2/5 (1 addressed, 0 open — atomic Journal::replace; commits 4ec8aac..52523a1)
Task 6: complete (commits 8a0cc3c..52523a1, review clean after 2 fix rounds; full ignored suite live-verified twice)
Task 6: minor (deferred): reconcile is belt-and-braces not total guarantee (crash-before-replace leaves SkipRemove cap, logged)
Task 6: minor (deferred): entries_for/replace TOCTOU under hypothetical multi-threaded caller
Task 6: minor (deferred): Journal::replace lacks debug_assert that entries belong to cgroup
Task 7: implemented be16f2c (impl-task7 died at rate limit; impl-task7b audited+committed); review round 1 — Critical: non-recursive member scan misses protected exes in descendant cgroups while freeze propagates to subtree (PLAN BRIEF ERRATA — spec's safety-over-coverage governs); Important: strip_cgroup_root unwrap_or_default matches-wrongly on "" rlm_base. Fix dispatched.
Task 7: minor (deferred): comm_of duplicates Name: parsing from parse_proc_status
Task 7: spec errata (mine): task-1/7 brief text "every process in the candidate cgroup" should read "in the candidate cgroup's SUBTREE" — freeze is hierarchical
Task 7: fix round 1/5 (2 addressed, 0 open — cgfs::pids_under recursive scan + Option rlm_base fail-closed; commits be16f2c..47915cc; re-reviewer live-ran nested-cgroup test independently)
Task 7: complete (commits afd6a76..47915cc, review clean after 1 fix round)
Task 8: implemented de2bcef; review round 1 — Critical: CLI Journal::open mutates cross-process (TOCTOU recover_tail can drop live daemon entry); Minor: false "read-only" comment. Fix dispatched (Journal::read_entries).
Task 8: fix round 1/5 (2 addressed, 0 open — Journal::read_entries read-only parse + accurate comment; commits de2bcef..e704a97; impl-task8 died at rate limit, impl-task8b completed)
Task 8: complete (commits 30f73e5..e704a97, review clean after 1 fix round)
PHASE 0 CODE TASKS 1-8 ALL COMPLETE. Task 9 = live gate (needs user) + docs.
FINAL WHOLE-BRANCH REVIEW (workflow, 17 agents, 5 lenses + adversarial verify): READY_WITH_FIXES. 7 survived / 4 refuted / 11 minors. 5 distinct defects: D1 rules enforcer reverts guard CAPS (frozen-skip covers freeze only) — spec-level; D2 prune uses eligibility as liveness proxy → self-defeating cap oscillation; D3 unlimit bucket is a valid guard target — spec-level; D4 swap.current read collapses cap to floor; D5 startup ordering defeats crash recovery when guard disabled. +3 promoted minors (inode sentinel, chain liveness, docs). Fix wave dispatched to fix-final. Report: final-review.md
Final fix wave: committed 717b5bc (D1-D5 + promoted minors A/B/C). Gates verified by controller: 108 passed/7 ignored, clippy -D warnings clean, fmt clean. Independent opus gate running.
Phase 0.5 plan written: docs/superpowers/plans/2026-08-01-phase05-harness.md (5 tasks: probe core+parsers, locked/touch modes, PSI integral, runner+hog, summary/compare).
WORKFLOW NOTE: two implementers held work uncommitted waiting on self-dispatched reviewers. Future dispatches must say: commit as soon as gates are green; your own reviewer is self-review, not a merge gate.
FINAL GATE (opus, 717b5bc): 6/7 findings ADDRESSED (D1-D5, A, C verified live-wired, not decorative). NOT-MERGE-READY on NEW-1 (Important REGRESSION): Promoted-B fix used oldest Cap for LIVENESS but disk holds newest Cap's our_high -> [Cap,Cap] chain SkipRemoves, cap never restored, record cleared = permanent pin (the exact failure Promoted A prevents). Old entries.last() handled it; fix regressed it. Also NEW-2 (minor regression: crash loop when guard disabled + no rules + unwritable state dir) and NEW-3 (minor: live_cgroups does full resolve incl. recursive pids_under per process every tick, discards all but cgroup path — storm hot path). Fix dispatched.
Note: "108 tests" = workspace sum (13 common + 4 cli + 91 rlm-core); gate reported rlm-core's 91. No discrepancy.
CONTROLLER ERROR (recorded): dispatched harness Task 1 and the NEW-1 regression fix concurrently against the SAME checkout — the SDD skill forbids parallel implementers for exactly this reason. h-impl-task1 handled it correctly (staged only harness/ + Cargo files); no damage. RULE GOING FORWARD: one implementer per checkout at a time, or isolation:worktree for genuinely parallel work.
Harness Task 1 complete: 21d06ed (probe loop + pure parsers; VmRSS 2772kB stable across a 5s run = no-allocation claim evidenced).
Gate correction: fix report's "108 passed" was CORRECT (workspace sum: common 13 + cli 4 + rlm-core 91). Gate misattributed common/cli as 0 and drew a trust inference from it; asked to withdraw.
Gate withdrew the trust remark and diagnosed its own root cause: it piped `cargo test --workspace` through `tail -60`, then grepped the truncated capture and mistook two doc-test result lines for the common/cli unit-test binaries whose output it had cut off. LESSON RECORDED: never truncate the output of a gate you intend to contradict a report with.
Test-count reference by tree state: 717b5bc = 108 (common 13 + cli 4 + rlm-core 91, 7 ignored). After harness crate + NEW-1 gate test = 113. Both correct for their commit; not drift.
Gate lesson 2 (sibling of the truncation one): in a concurrent session `git status` is a point-in-time read of a tree other agents are mutating — an untracked file is evidence only if still untracked on re-check. Gate reported a race as a defect (harness/ "untracked" mid-commit-window). Both errors share a root: asserting from a single unverified capture.
NEW-1/2/3 fix VERIFIED at 4415999 — gate MERGE-READY (static correctness). Gate independently reproduced the red/green (reverted rev().find() itself, saw both shipped + its own [Cap,Freeze,Cap] discriminator test FAIL, restored, passed). NEW-3 set-identity PROVEN structurally (6-step case analysis: dropped work feeds only verdict/coverage/unit, none reach the output; cache keyed on candidate.cgroup; finalize moves cgroup unmodified) — D2's liveness set is not narrowed. NEW-2 confirmed genuine Ok(()) not a relocated Err; rules==0 && enabled==true stays fatal by design.
ALL STATIC FINDINGS CLOSED: D1-D5, Promoted A/B/C, NEW-1/2/3. Gates 113 passed / 0 failed / 7 ignored, clippy+fmt clean.
PHASE 0 IS CODE-COMPLETE. Remaining and BLOCKED ON USER: (§4.1) live gate — real freeze/thaw stays in app-*.scope with memory.high intact, plus kill -9 mid-freeze then journal replay; (§4.2) swap.max=0 vs memory.high interaction — the guard's soft cap may not be soft on a cgroup that cannot reclaim anon.
GAP FOUND AND FILLED: harness Task 1 (21d06ed) was committed without its per-task review — I stopped its agent before dispatching a reviewer. Review dispatched now (h-review-task1), targeted at measurement-corrupting defects: pre-allocation capacity arithmetic, fixed-schedule vs cumulative drift, sleep(interval) vs sleep(interval - work).
Harness Task 1 review (21d06ed): NOT APPROVED — Important: parse failures silently zero-fill, so a broken schedstat/majflt read on the control arm reads as "no contention" and makes the comparison look better than reality. Minors: doc comment says schedstat field 8 (real: field 2 of 3 — typo inherited from MY plan text, plan also corrected), no --interval-ms 0 validation, loop can overrun duration by one interval. Fix dispatched.
Reviewer CONFIRMED the two things that most mattered: drift is anchored to a fixed schedule (start + n*interval, never recomputed from now), and the loop sleeps to an absolute deadline so /proc-read time is absorbed rather than accumulating. Both verified by hand-tracing and a live run. The no-allocation claim also holds structurally (resize once, index-assign, cleared+reserved buffers), not just by the RSS sample.
Harness Task 1 fix (31e8357) APPROVED — all 4 findings addressed; failure counts now in the OUTPUT header (line 1), not just stderr, so an automated consumer can tell a degraded run from a calm machine. Reviewer verified live (built release binary, ran it, checked line 1 and the --interval-ms 0 rejection).
CARRY-FORWARD to harness Task 2: Task 1's fix put {schedstat_failures, majflt_failures} on line 1, but Task 2 Step 3 specifies line 1 as {label, mode, interval_ms, working_set_mb, mlock_ok, slice}. Task 2 must MERGE both into ONE header object — a second header line would break the "line 1 = header, rest = Tick" contract both rely on.
NOTE for Task 4: tick DATA never exceeds duration_s now, but PROCESS lifetime still runs up to one interval_ms past it (deterministic bound). Size the systemd-run teardown/kill window off process exit, not tick data.
Harness Task 2 (71c1b2b) review: NOT APPROVED — two Importants, both measurement-integrity. (1) Asymmetric per-tick work: touch walks ~768 pages + reads 1MB every tick, locked does none, so touch's drift carries a constant additive offset that (touch-locked) misattributes to memory-induced stall. Reproduced twice idle: 98.2 vs 125.1 and 89.9 vs 102.0 us. FIX = make the control walk symmetric (locked pages can't fault, so CPU cancels and fault behaviour doesn't). (2) Default --file /proc/self/exe: readelf shows .text (886KB) sits inside the 1MB mmap, so touch-mode re-reads its own hot code pages, which LRU never reclaims — understates file-backed reclaim sensitivity, and Task 4 never overrides it. FIX = dedicated scratch file, fadvise DONTNEED, deleted on exit. Fix dispatched with a required before/after measurement (a fix that doesn't move the number is a failed fix).
Harness Task 2 fix (5e98919) re-review: both original Importants ADDRESSED and INDEPENDENTLY re-measured (reviewer's own 6 fresh trials: gap mean +3.6us, sign varies — systematic bias confirmed gone). NEW Important: zero-length regular file -> forced 1-byte mmap -> SIGBUS (exit 135) AND scratch file left on disk, falsifying the "deleted on every exit path" guarantee. Reachable via --file-mb 0 or an explicit empty --file. Fix dispatched (validate at parse time, reject zero-length mapping, RAII cleanup guard).
HARNESS NOISE FLOOR (measured, 6 idle trials, 5s/50ms defaults, ZERO pressure): (touch-locked) gap mean +3.6us, stdev ~38us, range -53..+43us. A single-pair delta must clear ~±40us (1σ) to ±70-80us (2σ) to be signal. Far below the 200ms perceptible-stall threshold so gross claims are safe, but Task 5's decompose must mark sub-floor deltas suspect/inconclusive rather than reporting 8us with the same confidence as 80us. Plan Task 5 being amended to take a noise-floor param.
Phase 1 plan written: docs/superpowers/plans/2026-08-01-phase1-preventive-tier.md (5 tasks). Chosen as this iteration's work because it is doc-only and cannot conflict with the in-flight harness Task 2 verification (which touches rlm-probe.rs). Key decisions baked in: memory.min chain covers init.scope (from the Phase 0 live discovery that systemd --user sits outside session.slice); ancestor-sum invariant with the monotone-but-undersummed case as an explicit test; drop-in MemoryMin values ship as PLACEHOLDERS with a comment naming the measurement that sets them (shipping invented numbers is the failure being avoided); daemon Slice=session.slice carries its MIR justification in the unit file; dynamic app.slice cap ships behind a default-off flag until the harness measures foreground-throttling cost; Task 5 gate requires any claimed win to clear the measured ~±40us noise floor or be reported inconclusive.
Harness Task 2 re-review (7e85a8b): SIGBUS genuinely fixed (all 3 repro cases clean, exit 135 gone). NEW Important: the RAII ScratchFileGuard is bypassed on the map_file error arm because std::process::exit() does not run destructors — reviewer reproduced with `ulimit -v 40000`, leaving a 50MiB scratch file on disk. Notable because an mmap failing under memory pressure is near the tool's actual operating conditions. Fix dispatched: restructure to run()->Result so guards drop before main exits (closes the class, not just the one path). Also ungating 3 hermetic integration tests wrongly marked #[ignore] (they need no systemd/cgroups, so the standard gate silently skipped them).
SIGKILL window assessed and accepted: scratch create->mmap->unlink completes in the first fraction of a second, so a SIGKILL during the measurement loop (nearly all of process lifetime) cannot leak; the sub-ms window before unlink is a genuine Drop-cant-run-on-SIGKILL limitation, documented not fixed.
/dev/null path changed but not regressed: zero-length check now intercepts before mmap, so one consistent diagnostic replaces two (ENODEV vs zero-length). Observable contract preserved.
Plan amendments VERIFIED binding: decompose's noise_floor_us is a required 3rd param with no default, Step 1 requires a 5us-vs-40us-floor suspect test, Step 3 pins the CLI default — a fresh implementer cannot pass the failing-tests-first gate without honouring it.
